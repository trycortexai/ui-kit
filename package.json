{
	"name": "ui-kit-monorepo",
	"scripts": {
		"build:sdks": "bunup",
		"build:static": "bun run --cwd static build",
		"build": "bun build:sdks && bun build:static",
		"build:dev": "bun build:sdks --env inline && bun build:static",
		"test:chat": "IFRAME_SRC=http://localhost:5000 bun run build:dev && bun --port 5000 static/dist/chat/index.html & sleep 1 && bun test/chat.html",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"tsc": "tsc --noEmit",
		"release": "bumpp -r --commit --push --tag",
		"prepare": "bun simple-git-hooks"
	},
	"devDependencies": {
		"@biomejs/biome": "2.2.5",
		"@bunup/plugin-tailwindcss": "^0.14.20",
		"@types/bun": "^1.2.23",
		"bumpp": "^10.3.1",
		"bunup": "^0.14.20",
		"simple-git-hooks": "^2.13.1",
		"typescript": "^5.9.3"
	},
	"simple-git-hooks": {
		"pre-commit": "bun run lint && bun run tsc"
	},
	"workspaces": [
		"static",
		"packages/*"
	]
}
